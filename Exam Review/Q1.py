'''
Using a variable for length and a variable for width, create a program that
will calculate the area of a rectangle, storing it in a variable and outputting
the result (without units).
'''

length = float(input())
width = float(input())
area_regtangle = length * width
print(area_regtangle)